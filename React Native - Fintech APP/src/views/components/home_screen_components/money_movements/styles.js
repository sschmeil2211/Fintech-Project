import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    movementsContainer: {
        marginHorizontal: 15,
        marginVertical: 5,
    },
    textStyle: {
        fontSize: 12,
        color: "#4A494F",
        fontWeight: "400"
    },
    voidMovementsContainer: {
        position: "absolute",
        alignItems: "center",
        marginVertical: 100,
        height: 100,
        width: "100%"
    },
    voidMovementsText: {
        color: "#4A494F"
    }
});

export default styles;  