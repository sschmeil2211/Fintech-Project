import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    iconColumn: {
        flexDirection: "column",
        alignItems: "center",
    },
    text: {
        marginTop: 5,
        fontSize: 10,
        fontWeight: "bold",
        color: "#8D888F"
    },
    iconBox: {
        backgroundColor: "#231E22",
        height: 50,
        width: 50,
        justifyContent: "center",
        alignItems: "center",
        borderRadius: 50
    },
});

export default styles; 