import { StyleSheet } from "react-native";

const styles = StyleSheet.create({ 
    button: {   
        alignItems: "center", 
        paddingVertical: 15,
        margin: 10,
        borderRadius: 10,
        bottom: 25
    },
    buttonLabel: {
        color: "white",
        fontWeight: "bold"
    }
});

export default styles; 