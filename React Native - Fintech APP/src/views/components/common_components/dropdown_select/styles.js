import { StyleSheet, Dimensions } from "react-native";

const styles = StyleSheet.create({
    container: {  
        marginTop: 15, 
    },
    text: {
        color: "white",
        fontSize: 12,
        marginHorizontal: 20, 
    },
    scrollView: {
        maxHeight: 150,
    }
});

export default styles; 